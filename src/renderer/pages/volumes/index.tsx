import Layout from "../../components/layout";
// import Volumes from "@/src/components/sideNavComponenets/volumes";
import React from "react";
import Volumes from "renderer/components/sideNavComponenets/volumes";

const Container = () => {
  return (
    <Layout>
      <Volumes />
    </Layout>
  );
};

export default Container;
